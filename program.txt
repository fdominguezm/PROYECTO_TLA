CLASSES {
  // se crea la tabla House
  House {
    int size;
    int rooms;
    bool for_sale;
    string address;
  }
  // se crea la tabla Person
  Person {
    string name;
    int age;
    bool married;
  }
}

int MiNumero = 20;

MiNumero = "Jose";

int myFunction(int n1, int n2) {
  b = n1 * 2;
  return b * n2;
}

Person juampi = new Person(23, "Juampi", true);

int a = 28;
string julian = "julian";

Person aPerson = new Person("Benja", 23, false);

Person bPerson = new Person("Chule", 21, false);

House aHouse = new House(40, 2, false, "Luis Maria Campos 1386");

aPerson.name = "Fausto";
bPerson.name = aPerson.name;

if (aPerson.age > 22) {
  b = aPerson.age;
  pito = b;
  Person cPerson = new Person("Juan", pito, true);
}

bPerson.age = CLASSES.House.max(size);

// elimino la tupla aPerson de la tabla de personas
aPerson.delete()